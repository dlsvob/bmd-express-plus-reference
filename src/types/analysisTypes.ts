// src/types/analysisTypes.ts (or your equivalent)

export interface LegendItem {
    label: string; // The text displayed in the legend (e.g., "Cluster 1", "circle", "10")
    value: string | number; // The actual value used for styling (e.g., "#ff0000", "circle", 10)
}

// ... other analysis-related types can also go here ...
